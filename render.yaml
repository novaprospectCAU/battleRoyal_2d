services:
  - type: web
    name: battle-royal-2d-server
    env: node
    plan: starter
    rootDir: .
    buildCommand: npm install && npm run build
    startCommand: npm run start -w @battle-royal/server
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
